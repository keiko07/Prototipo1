<form method="post" action="{{ path('gestion_registro_delete', {'id': gestion_registro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gestion_registro.id) }}">
    <button class="btn">Delete</button>
</form>
